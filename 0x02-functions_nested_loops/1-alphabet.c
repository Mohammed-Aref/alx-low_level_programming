#include "main.h"

/**
 * main - Entry point
 * Description: C programm prints alphabet using prototype
 * Return: Always 0 sucess)
*/

int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		_putchar(ch);
	}
	_putchar('\n');

	return (0);
}
